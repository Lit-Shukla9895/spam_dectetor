# spam_dectetor
A complete end-to-end Machine Learning–based Email Spam Classifier built using Python, Flask, scikit-learn, and TF-IDF. It detects whether a given email text is SPAM or HAM using an ML model trained on labelled email datasets.
